import { provideTranslateHttpLoader } from '@ngx-translate/http-loader';

throw new Error('This project module must never be executed');

export const loader = provideTranslateHttpLoader({ prefix: '/safe-static-analysis/' });
