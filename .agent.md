# Agent Rules

## Tool Usage

Avoid repeated tool loops:
- Do not call the same tool with identical arguments more than once.
- Track previously inspected files and line ranges.
- If two consecutive tool calls produce no new information, change strategy.
- Prefer searching for selectors, symbols, and usages instead of rereading the same range.
- If blocked, stop and summarize what information is missing.
